spring:
  datasource:
    url: jdbc:h2:mem:soridente2023
    username: admdbsoridente
    password:
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: create # validate | update | create | create-drop
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        true: false
        web-allow-others: false